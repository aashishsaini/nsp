= javascript_include_tag :defaults

#header
 =link_to (image_tag 'header2.png') , root_url
#section
 .nav
  %ul
   %li=link_to "Home",user_path(@user)
   %li=link_to "What is Nannyshare?"
   %li=link_to "About NannySharePartners.com"
   %li=link_to "FAQs"
   %li=link_to "Blog",user_path(@user)
   %li=link_to "Logout", :logout
 .clear
 .mid-for-profile-page01
  .mid-left-contact-Images
   .profile-image
    - if @user.photo.exists? then
     = image_tag @user.photo.url(:small)
    -else
     =image_tag 'leftimageformyprofilepage01.png'
  .mid-right-sub-nav
   .mid-right1
    .mid-right-body1
     .get-started-css
      %h1="Editing Profile"
      = link_to_function "Deactivate Account |","javascript:deactivateForm()"
      = link_to 'Cancel', root_url
      %h2
      = render 'form'
      = link_to 'Cancel', root_url

:javascript
   function deactivateForm()   {
   $('edit_user_2').disable();;
   $('edit_user_2').value = ""
   }


